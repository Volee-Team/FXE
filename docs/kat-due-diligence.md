# Kat's due-diligence questions: where each answer lives

Kat's 18 questions (relayed by Alex 2026-08-14, `docs/prompt-log/2026-08-backfilled.md` line 803: "we should be considering everything ... since before EVERYTHING was just in a google doc"), each mapped to the artifact that answers it today. Written 2026-09-13; living file, re-derive every cell by command before trusting it.

The 2026-08-14 audit graded this list **C** and its per-item detail was truncated at 8 KB in the log; the 2026-08-15 reply promised a fix per item ("Kat's list: how we fix it", line 1596) and nothing recorded which landed. This table is that record. Status: **answered** (an artifact a PM can be handed), **partial** (pieces exist across files, no single answer), **not started**.

| # | Question (Kat's wording) | Where the answer lives today | Status | Owner and next step |
|---|---|---|---|---|
| 1 | "Do you have an architecture diagram for the tools and/or services that you used to develop the app?" | `docs/architecture.md` §3, a Mermaid flowchart: iOS app, web admin, Supabase (Auth, Postgres 17 + RLS, narrow views, RPCs), edge functions, GitHub Actions. Regenerated 2026-09-01, refreshed 2026-09-12 | answered | [me] Redraw when a box changes (the `push` edge function will be the next one). Hand Kat §1–§3 |
| 2 | "What framework did you use?" | `docs/architecture.md` §4 (SwiftUI, iOS 17, XcodeGen, supabase-swift 2.41.1, stripe-ios 24.25.0 PaymentSheet only), §5 (Supabase, Postgres 17), §8 and `docs/web-admin.md` (static HTML + supabase-js on Vercel, no build step). Pinned versions in `project.yml` | answered | none |
| 3 | "Did you build to allow for scale?" | `docs/engineering-design.md` §12: "we deliberately did not build for one"; a club is hundreds of players, Supabase is ~100x oversized. The one contended path is locked and raced: `docs/architecture.md` §6, `tests/sql/capacity_race.sh` (12-way per run, 24-way verified 2026-07-28) | answered | [me] `docs/launch-checklist.md` §E item 7: seed 60 clinics once and screenshot the list, so "scale" has one artifact beyond the race |
| 4 | "Is there a CMS tool used to manage images, content?" | No, by decision: `docs/roadmap.md` "Deliberately not doing" ("There is no managed content. Clinic descriptions live on the clinic"). Tara's words are versioned in `docs/copy.md`; the app has no images beyond the icon and the mark | answered | none |
| 5 | "Do you have wireframes?" | Tara's two AI-made mockups in `media/` (FXE 1 and FXE 2, PDF + PNG), treated as a style guide and never a spec (CLAUDE.md "On the wireframe mockups", "Which source wins" #4). No wireframes of the screens as built; `docs/architecture.md` §4 names the `media/` folder and what it is for | partial | [me] The screenshot set in row 6 is the wireframe of record. The `media/` pointer promised here landed in `docs/architecture.md` §4 on 2026-09-12. |
| 6 | "How are you tracking U/I changes" | Text, not pictures: the CLAUDE.md changelog (one entry per session), `docs/feature-review-2026-09-02.md` (every screen walked, one finding per line with a status column), `docs/copy-approved.txt` + the `copy-gate` CI job for every user-visible string, `docs/design-system.md` for tokens, and `git log --follow` on `FXETennis/Views/`. 2026-08-15 promised a "screenshot set per release"; none exists (`.claude/commands/` holds only `probes.md`; the `/screens` command in `docs/dev-practices-for-john.md` is Volee's) | partial | [me] A `docs/screens/<tag>/` folder captured from the simulator at every TestFlight upload, alongside the tag in row 7 |
| 7 | "Is the app tagged? If so, do you have a tag spec?" (read as release tags) | `git tag \| wc -l` = 0. The spec is one row: `docs/launch-checklist.md` §C item 10, an annotated tag `v0.1.0-tf1` plus a changelog line at every TestFlight upload; Alex asked for tags on 2026-08-13 and Volee once shipped a build no commit matched | not started | [me] First tag at the first upload (blocked on Apple enrollment, §C item 1). No build has shipped, so nothing is untagged yet |
| 8 | "Is the app tagged? ... What tool did you use?" (read as analytics tagging) | Nothing tags events. `docs/engineering-design.md` §11: no third-party analytics SDK; `docs/web-admin.md` §5: "No analytics. No error reporting service." 2026-08-15 promised "an `events` table, no third-party SDK"; not built, not in `docs/roadmap.md` | not started | [Alex] Ask Kat which reading she meant. If analytics: a roadmap row first, and a Tara question (what does she want to know?) before any table, per hard rule 14 |
| 9 | "Is there an admin view?" | Yes, two. The Manage tab in the app (`FXETennis/Views/AdminClinicsView.swift`, `AdminClinicDetailView.swift`, `PlayersDirectoryView.swift`) and the web admin at fxe-tennis-admin.vercel.app (`web/index.html`, design record `docs/web-admin.md`, status table in `docs/roadmap.md` "Web admin"). Gate is `is_admin()` in Postgres, `docs/architecture.md` §5. Tested by 5 admin XCUITests and 12 Playwright tests (`web/tests/admin.spec.mjs`) | answered | none. On 2026-08-14 the honest answer was "no admin view at all"; built 2026-08-15 (phone) and 2026-08-28 (web) |
| 10 | "What data are you capturing?" | `FXETennis/Resources/PrivacyInfo.xcprivacy`: name, email, phone, user id, linked to identity, no tracking; card details go to Stripe and never to us. Screen by screen: sign-up profile, Edit details, the card screen (`docs/architecture.md` §4) | partial | [me] A PII inventory table in `docs/architecture.md` §5: column, why it is collected, who can read it, retention. Promised 2026-08-15, not written |
| 11 | "What data are you storing?" | `docs/architecture.md` §5 "The tables": 15 tables and every enum, one line each, plus which views expose what to whom. `docs/engineering-design.md` "Data model" is the June-era list and carries a 2026-09-12 drift note | partial | Same inventory as row 10. Also `docs/launch-checklist.md` §C item 5 (account deletion: what it deletes is a Tara question) and §D item 9 (export if she leaves) |
| 12 | "Do you have a data architecture doc?" | `docs/architecture.md` §5 (tables, views, RPCs, the grant model, regenerated from `pg_class` / `pg_proc` 2026-09-01) plus the 27 files in `supabase/migrations/`, which are the schema. No ER diagram, no column-level listing outside the migrations. 2026-08-15 promised "generate it from the live schema, one session"; §5 is what landed | partial | [me] An ER diagram in `docs/architecture.md` §5 (Mermaid `erDiagram`, generated from the live schema so it cannot drift by hand) |
| 13 | "Is the source code stored in GitHub?" | github.com/Volee-Team/FXE, branch `main`, public. 118 commits since 2026-08-08, 39 PRs (`git rev-list --count HEAD`, `gh pr list --state all`). Where to start: `README.md` | answered | [Alex] The repo being public is why the backup artifacts were readable (`docs/launch-checklist.md` §D item 10); private or not is his call |
| 14 | "How are you managing approvals & change requests?" | Code: branch, PR, CI gate. `.github/workflows/probes.yml` runs 9 jobs on every push and PR; branch protection on `main` requires "SQL probes + concurrency" and "Build iOS app + unit tests" green (`gh api repos/Volee-Team/FXE/branches/main/protection`), 0 required reviewers, not enforced for admins (`docs/architecture.md` §9); the `migration-immutability` job refuses edits to an applied migration. Product: Tara's asks land in `docs/roadmap.md` first ("Nothing gets built straight off a text message"), her calls in `docs/decisions/` (superseded by a new record, never rewritten in substance; the only in-place edits are forward-pointing supersede notes and corrections to a "Pinned by" section), her open questions in `docs/questions-for-tara.md` (hard rule 14); words in `docs/copy-review.md` until Alex ticks them | partial | [me] The pieces exist across five files and no one paragraph says "how a change gets in". Add that paragraph to `README.md`. A second reviewer waits for a second developer |
| 15 | "Error logs, production bugs, is this history stored somewhere? Jira?" | Bug history: `docs/backlog.md` (Open and Fixed tables, dated, with the fix and the command that found it), the CLAUDE.md changelog, `docs/feature-review-2026-09-02.md`. No Jira; 0 GitHub Issues (`gh issue list --state all`). Error logs: none. No crash reporting, no error monitoring (`docs/launch-checklist.md` §D item 6 "none"; `docs/whats-next.md` item 3); edge-function logs exist only in the Supabase dashboard. No member has used hosted, so there is no production bug history to store yet. 2026-08-15 promised crash reporting "after the admin surface, before real members" | partial | [me]/[Alex] §D item 6: pick Sentry or Supabase logs + an alert on edge-function errors, before the first TestFlight build. The tracker stays `docs/backlog.md` |
| 16 | "Have you run the app through an accessibility review?" | No review has been run. Foundation: `docs/design-system.md` "The accessibility contract" (status never by colour alone, `StatusChip` with VoiceOver labels) and "Accessibility: measured WCAG contrast" (every pair passes, palette B, 2026-08-12); 65 `accessibilityIdentifier` and 20 label / Dynamic Type uses in `FXETennis/` (`grep -rn`). `docs/launch-checklist.md` §E item 4: "the design system promises this; nothing checks it" | not started | [me] §E item 4: VoiceOver on every screen, Dynamic Type at the largest size, and an XCUITest that fails on any button with an empty accessibility label |
| 17 | "What if any API's have you explored between UTR & USTA" | None explored. The app uses the USTA NTRP scale as a self-reported rating (`docs/ntrp-chart.md`, `FXETennis/Models/NTRPRating.swift`, CLAUDE.md decisions 6+7), same storage as Volee. No row in `docs/roadmap.md`. Kat: "you don't need to really do any of these" | not started | [Alex] One line under `docs/roadmap.md` "Parked" so it is not re-asked; a Tara question (does a verified rating matter to her?) before anyone reads an API doc |
| 18 | Patch notes (Alex, 2026-08-13: "do we have patch notes anywhere ... I think what kat wanted") | No CHANGELOG file, no release notes. The CLAUDE.md changelog is per session, not per build. `docs/launch-checklist.md` §C item 10 pairs a changelog line with each tag | not started | [me] With row 7: a CHANGELOG keyed by tag, first entry at the first TestFlight upload |

Landed from the 2026-08-15 plan: admin view (row 9), PR + branch protection (row 14), architecture doc from the live schema (row 12, as far as §5). Not landed: events table (8), screenshot set (6), PII inventory (10), crash reporting (15), accessibility pass (16). Privacy policy and account deletion were in the same plan and sit in `docs/launch-checklist.md` §C items 5 and 6.

## Not found in the log

Every question above is in Kat's list verbatim except row 18, which is Alex's attribution ("what kat wanted") rather than her wording. Two things the log cannot answer: whether "tagged / tag spec / what tool" means release tags or analytics tagging (rows 7 and 8 cover both), and the audit's per-item verdicts, which were cut at 8 KB when the transcript was backfilled.
