# Style guide

**Source of truth for every visual decision from the day Kat's guide lands
here** (Alex, 2026-09-22). Until she sends it, this file is a placeholder and
`docs/design-system.md` plus `FXETennis/Resources/Brand.swift` stand.

When it arrives:

1. Paste it here verbatim, dated, with her name.
2. Diff it against `docs/design-system.md` and `Brand.swift`; every token
   that differs changes in `Brand.swift` and `web/tokens.css` in one PR, with
   screenshots of the screens it touches.
3. Re-check the 2026-09-22 changes made before it arrived (the page
   gradient, the Profile rows, the Sign Out link, the Testing tab) against it.
4. Tara's standing ask stays unless the guide says otherwise: pages carry a
   light cream-to-warmer-cream gradient, not flat white.
