{
  "name": "fxe-tennis-web-admin",
  "private": true,
  "description": "Static web admin for FXE Tennis. No build step; package.json exists only for the browser tests.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
